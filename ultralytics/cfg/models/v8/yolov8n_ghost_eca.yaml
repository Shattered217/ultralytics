# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license
# YOLOv8n with Ghost modules and ECA attention
# ============================================================================
# 消融实验配置 (E4: Ghost + ECA)
# 
# 本配置唯一改变点：组合 Ghost 轻量化和 ECA 注意力机制
# 
# 改动详情：
# - backbone 和 head 的 C2f → C3Ghost（来自 yolov8n_ghost.yaml）
# - 每个 C3Ghost 后插入 ECA 模块（来自 yolov8n_eca.yaml）
# - 这是 E1 (Ghost) 和 E2 (ECA) 的组合实验
# - 其他所有层保持与 baseline 完全一致
# 
# 用途：验证轻量化模块和注意力机制组合的效果
# 对应实验：E4 (Ghost + ECA)
# ============================================================================

# Parameters
nc: 80  # number of classes
scales: # model compound scaling constants
  # [depth, width, max_channels]
  n: [0.33, 0.25, 1024]  # YOLOv8n-ghost-eca: fewer params than baseline but with attention

# YOLOv8n backbone with Ghost + ECA
backbone:
  # [from, repeats, module, args]
  - [-1, 1, Conv, [64, 3, 2]]       # 0-P1/2
  - [-1, 1, Conv, [128, 3, 2]]      # 1-P2/4
  - [-1, 3, C3Ghost, [128, True]]   # 2 ← Ghost
  - [-1, 1, ECA, [128]]             # 3 ← ECA
  - [-1, 1, Conv, [256, 3, 2]]      # 4-P3/8
  - [-1, 6, C3Ghost, [256, True]]   # 5 ← Ghost
  - [-1, 1, ECA, [256]]             # 6 ← ECA
  - [-1, 1, Conv, [512, 3, 2]]      # 7-P4/16
  - [-1, 6, C3Ghost, [512, True]]   # 8 ← Ghost
  - [-1, 1, ECA, [512]]             # 9 ← ECA
  - [-1, 1, Conv, [1024, 3, 2]]     # 10-P5/32
  - [-1, 3, C3Ghost, [1024, True]]  # 11 ← Ghost
  - [-1, 1, ECA, [1024]]            # 12 ← ECA
  - [-1, 1, SPPF, [1024, 5]]        # 13

# YOLOv8n head with Ghost (调整索引)
head:
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 8], 1, Concat, [1]]       # cat backbone P4 (原 6 → 8)
  - [-1, 3, C3Ghost, [512]]         # 16 ← Ghost

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 5], 1, Concat, [1]]       # cat backbone P3 (原 4 → 5)
  - [-1, 3, C3Ghost, [256]]         # 19 (P3/8-small) ← Ghost

  - [-1, 1, Conv, [256, 3, 2]]
  - [[-1, 16], 1, Concat, [1]]      # cat head P4 (原 12 → 16)
  - [-1, 3, C3Ghost, [512]]         # 22 (P4/16-medium) ← Ghost

  - [-1, 1, Conv, [512, 3, 2]]
  - [[-1, 13], 1, Concat, [1]]      # cat head P5 (原 9 → 13)
  - [-1, 3, C3Ghost, [1024]]        # 25 (P5/32-large) ← Ghost

  - [[19, 22, 25], 1, Detect, [nc]] # Detect(P3, P4, P5)
